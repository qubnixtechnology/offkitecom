$kind: http-request
method: GET
url: '{{baseUrl}}/profile'
order: 1000
headers:
  - key: Accept
    value: application/json
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{authToken}}'
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Get profile returns success', function () {
        pm.expect(pm.response.code).to.equal(200);
      });
